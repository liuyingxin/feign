<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="my.dao.CreditAuditorMapper">

    <resultMap type="my.entity.CreditAuditor" id="CreditAuditorMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="mobile" column="mobile" jdbcType="VARCHAR"/>
        <result property="manageCityList" column="manage_city_list" jdbcType="VARCHAR"/>
        <result property="valid" column="valid" jdbcType="INTEGER"/>
        <result property="channel" column="channel" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="CreditAuditorMap">
        select
          id, user_name, mobile, manage_city_list, valid, channel, create_time, update_time
        from uxin_fin.credit_auditor
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="CreditAuditorMap">
        select
          id, user_name, mobile, manage_city_list, valid, channel, create_time, update_time
        from uxin_fin.credit_auditor
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="CreditAuditorMap">
        select
          id, user_name, mobile, manage_city_list, valid, channel, create_time, update_time
        from uxin_fin.credit_auditor
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userName != null and userName != ''">
                and user_name = #{userName}
            </if>
            <if test="mobile != null and mobile != ''">
                and mobile = #{mobile}
            </if>
            <if test="manageCityList != null and manageCityList != ''">
                and manage_city_list = #{manageCityList}
            </if>
            <if test="valid != null">
                and valid = #{valid}
            </if>
            <if test="channel != null and channel != ''">
                and channel = #{channel}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into uxin_fin.credit_auditor(user_name, mobile, manage_city_list, valid, channel, create_time, update_time)
        values (#{userName}, #{mobile}, #{manageCityList}, #{valid}, #{channel}, #{createTime}, #{updateTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update uxin_fin.credit_auditor
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="mobile != null and mobile != ''">
                mobile = #{mobile},
            </if>
            <if test="manageCityList != null and manageCityList != ''">
                manage_city_list = #{manageCityList},
            </if>
            <if test="valid != null">
                valid = #{valid},
            </if>
            <if test="channel != null and channel != ''">
                channel = #{channel},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

  

</mapper>